pandas
scikit-learn
streamlit
matplotlib

  To run the project:

1. Install dependencies
pip install -r requirements.txt

2. Train the model
python train-model.py

3. Run the app
streamlit run app.py
